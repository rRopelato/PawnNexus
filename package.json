{
  "name": "pawnnexus",
  "private": true,
  "version": "0.1.0",
  "description": "PawnNexus community site for Dragon's Dogma 2 pawns.",
  "workspaces": [
    "frontend",
    "worker"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:worker": "npm run dev -w worker",
    "build": "npm run build -w frontend && npm run build -w worker",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w worker",
    "lint": "npm run lint -w frontend && npm run lint -w worker"
  },
  "engines": {
    "node": ">=20"
  }
}
